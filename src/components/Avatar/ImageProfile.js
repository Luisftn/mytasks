import "./imageprofile.sass";

function ImageProfile(){
    return(
        
        <header className="container">
            <h1 className="profile"></h1>
        </header>
    );
}

export default ImageProfile;